using System;

namespace ejercicio4
{
    class Program
    {
        static void Main(string[] args)
        {
            int[] vNros;

            VNros = new int[10];

            int aux;

            for(int x = 0; x < 10; x+){
            hros[x] = int.Parse(Console.ReadLine());

            }

            for(int x = 0; x < 10; x+){
            
            for(int y = 0; y < 9; y+#){

            
                if (viros[y] > vNros[y+1]){
                aux = vhiros[y];
                iros[y] = vNros[y+1];
                iros[y+1] = aux;
            }

            }
            Console.writeLine("Vector:");
            for(int x = 0; x < 10; x+){

            Console.wWriteLine(vNros[x]);

            }

        }

            

        }

    }
}
